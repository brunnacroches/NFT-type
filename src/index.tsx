import React from "react";
import ReactDOM from "react-dom";
import Home from "./pages/Home";

import "./global.css";

// primeiro argumento que vai renderizar, render ( é  a funcao ) e ele vai pegar
// o parametro
ReactDOM.render(<Home />, document.getElementById("root"));

// ele vai renderizar a pagina home na div que tem no html
// pegar o componete home e vai renderizar dentro da div

//
